<%=form_for @social do |f| %>
 <% if @social.errors.any? %>
	  <div id="error_explanation">
	    <h2><%= pluralize(@social.errors.count, "error") %> prohibited
	      this social from being saved:</h2>
	    <ul>
	    <% @social.errors.full_messages.each do |msg| %>
	      <li><%= msg %></li>
	    <% end %>
	    </ul>
	  </div>
    <% end %>
 

  
 
    <p>
      <%= f.label :title %><br>
      <%= f.text_field :title ,class: "form-control title" ,placeholder: "title.." %>
    </p>
    <p>
    <%= f.label :body %><br>
    <%= f.text_area :body,class: "form-control body1",placeholder: "body.." %>
    </p>
    


    <p>

    <%= f.label :image %><br>
    <%= f.file_field :image,class: "form-control image" %>
    </p>
   
    <%= f.submit "submit" ,class: "btn btn-primary submitt"  %>

<% end %>
<script>
    $(document).ready(function(){
        $(".submitt").on("change",function(){
        var title = $('.title').val();
        var body1 = $('.body1').val();
        var image = $('.image').val();
        
       alert(u)
        });
    });


</script>